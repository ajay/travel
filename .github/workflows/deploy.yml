name: deploy

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ajay/site-common/.github/workflows/deploy.yml@main
